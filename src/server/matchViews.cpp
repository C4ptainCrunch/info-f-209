void playerSelection(JsonObject * json, UserHandler * thread){
    char * reponse;
}

void playerAction(JsonObject * json, UserHandler * thread){ // si un joueur est selectionné, montre les actions possibles (mouvement, taper cognard, passe, ...)
    char * reponse;
}

void selectCase(JsonObject * json, UserHandler * thread){
    char * reponse;
}

void getPlayerDeplacement(JsonObject * json, UserHandler * thread){ // reçoit l'entrée de la destination d'un joueur se déplaçant
    char * reponse;
}

void playerCatcheSnitch(JsonObject * json, UserHandler * thread){
    char * reponse;
}

void playerHitBudger(JsonObject * json, UserHandler * thread){ // reçoit l'action frapper cognard
    char * reponse;
}

void doPass(JsonObject * json, UserHandler * thread){ //reçoit l'action de tentative de passe vers un autre joueur de l'équipe
    char * reponse;
}

void doInterception(JsonObject * json, UserHandler * thread){
    char * reponse;
}