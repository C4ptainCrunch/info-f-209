/*#ifndef MATCHVIEWS_H
#define MATCHVIEWS_H

void playerSelection(JsonValue * json, UserHandler * thread);
void playerAction(JsonValue * json, UserHandler * thread);
void getPlayerDeplacement(JsonValue * json, UserHandler * thread);
void playerCatcheSnitch(JsonValue * json, UserHandler * thread);
void playerHitBudger(JsonValue * json, UserHandler * thread);
void doPass(JsonValue * json, UserHandler * thread);
void doInterception(JsonValue * json, UserHandler * thread);

#endif // MATCHVIEWS_H*/