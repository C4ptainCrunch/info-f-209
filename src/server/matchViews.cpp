void playerSelection(JsonNode * json, UserHandler * thread){
    char * reponse;
}

void playerAction(JsonNode * json, UserHandler * thread){ // si un joueur est selectionné, montre les actions possibles (mouvement, taper cognard, passe, ...)
    char * reponse;
}

void selectCase(JsonNode * json, UserHandler * thread){
    char * reponse;
}

void getPlayerDeplacement(JsonNode * json, UserHandler * thread){ // reçoit l'entrée de la destination d'un joueur se déplaçant
    char * reponse;
}

void playerCatcheSnitch(JsonNode * json, UserHandler * thread){
    char * reponse;
}

void playerHitBudger(JsonNode * json, UserHandler * thread){ // reçoit l'action frapper cognard
    char * reponse;
}

void doPass(JsonNode * json, UserHandler * thread){ //reçoit l'action de tentative de passe vers un autre joueur de l'équipe
    char * reponse;
}

void doInterception(JsonNode * json, UserHandler * thread){
    char * reponse;
}