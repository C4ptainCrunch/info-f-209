// TO DO : mettre communication entre thread (pour les erreurs)

void inviteToMatch(JsonValue * json, UserHandler * thread){
    char * reponse;
}

void reponseToMatchInvitation(JsonValue * json, UserHandler * thread){
    char * reponse;
}

void viewUserList(JsonValue * json, UserHandler * thread){
    char * reponse;
}

void goToManagement(JsonValue * json, UserHandler * thread){
    char * reponse;
} //amène à l'écran de gestion du club

void buildNewInstallation(JsonValue * json, UserHandler * thread){
    char * reponse;
}

void manageInstallation(JsonValue * json, UserHandler * thread){
    char * reponse;
} //amène à la gestion de l'installation

void manageTeam(JsonValue * json, UserHandler * thread){
    char * reponse;
}

void goToAuctionRoom(JsonValue * json, UserHandler * thread){
    char * reponse;
}

void viewAuctions(JsonValue * json, UserHandler * thread){
    char * reponse;
}

void bidToAuction(JsonValue * json, UserHandler * thread){
    char * reponse;
} // nécessite un id par enchère. l'id de l'enchère doit etre mentionné dans le message

void viewUserBids(JsonValue * json, UserHandler * thread){
    char * reponse;
}

void makeAuction(JsonValue * json, UserHandler * thread){
    char * reponse;
}