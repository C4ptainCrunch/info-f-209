//#include "matchViews.h"

/*void playerSelection(JsonValue * json, UserHandler * thread){
    char * reponse;
}

void playerAction(JsonValue * json, UserHandler * thread){ // si un joueur est selectionné, montre les actions possibles (mouvement, taper cognard, passe, ...)
    char * reponse;
}

void selectCase(JsonValue * json, UserHandler * thread){
    char * reponse;
}

void getPlayerDeplacement(JsonValue * json, UserHandler * thread){ // reçoit l'entrée de la destination d'un joueur se déplaçant
    char * reponse;
}

void playerCatcheSnitch(JsonValue * json, UserHandler * thread){
    char * reponse;
}

void playerHitBudger(JsonValue * json, UserHandler * thread){ // reçoit l'action frapper cognard
    char * reponse;
}

void doPass(JsonValue * json, UserHandler * thread){ //reçoit l'action de tentative de passe vers un autre joueur de l'équipe
    char * reponse;
}

void doInterception(JsonValue * json, UserHandler * thread){
    char * reponse;
}*/