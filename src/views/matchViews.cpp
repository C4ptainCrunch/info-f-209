void playerSelection(JsonDict * json, UserHandler * thread){
    char * reponse;
}

void playerAction(JsonDict * json, UserHandler * thread){ // si un joueur est selectionné, montre les actions possibles (mouvement, taper cognard, passe, ...)
    char * reponse;
}

void selectCase(JsonDict * json, UserHandler * thread){
    char * reponse;
}

void getPlayerDeplacement(JsonDict * json, UserHandler * thread){ // reçoit l'entrée de la destination d'un joueur se déplaçant
    char * reponse;
}

void playerCatcheSnitch(JsonDict * json, UserHandler * thread){
    char * reponse;
}

void playerHitBudger(JsonDict * json, UserHandler * thread){ // reçoit l'action frapper cognard
    char * reponse;
}

void doPass(JsonDict * json, UserHandler * thread){ //reçoit l'action de tentative de passe vers un autre joueur de l'équipe
    char * reponse;
}

void doInterception(JsonDict * json, UserHandler * thread){
    char * reponse;
}